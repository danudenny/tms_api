import { Branch } from '../../../shared/orm-entity/branch';

describe('master-branch-list', () => {
  let branchs: Branch[];

  beforeAll(async () => {
  });

  it('Valid list', () => {

  });

  it('Verify created branch', () => {

  });
});
